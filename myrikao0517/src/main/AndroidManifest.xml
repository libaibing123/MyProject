<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myrikao0517">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WebViewActivity" />
        <activity android:name=".ui.classfiy.ListDetailsActivity" />
        <activity android:name=".ui.classfiy.BannerDetailsActivity" />
        <activity android:name=".ui.classfiy.ListActivity" />
        <activity android:name=".ui.shopcart.ShopCartActivity" />
        <activity android:name=".ui.mine.MakeSureOrderActivity"></activity>
    </application>

</manifest>